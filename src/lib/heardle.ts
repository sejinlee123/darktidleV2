/** Max value stored for Heardle streak fields (DB + leaderboard). */
export const HEARDLE_STREAK_CAP = 999;

/** Users with `lastSeenAt` within this window count as “online” on the home page. */
export const ONLINE_WINDOW_MS = 5 * 60 * 1000;
